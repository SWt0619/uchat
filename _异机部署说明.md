# Uchat 异机部署说明（把一个实例放到另一台电脑）

> 适用于：想让另一台电脑再开一个「16 人聊天室」。每个实例是**完全独立**的
> （各自一份 data/ ⇒ 账号、聊天记录、通话互不相通）。
> 本说明对应 **v2.8.5 起的包**；早期包（≤ v2.7.2）没有"静默运行"那一套文件，见 §一 的提示。

## 一、包里有什么 / 缺什么

| 件 | 在包里 | 说明 |
|---|---|---|
| `uchat.jar` | ✅ | 服务本体 |
| **`静默运行Uchat.vbs`** + `静默运行Uchat.ps1` / `静默监视Uchat.ps1` / `静默停止Uchat.ps1` / `_run_uchat_service.cmd` | ✅（v2.8.2 起） | **推荐用法**：双击 `静默运行Uchat.vbs` —— 无窗口运行，右下角托盘出现状态图标（🟢 正常 / 🟡 抖动 / 🔴 无响应，悬停看运行时长，右键可 打开页面 / 看日志 / 立即检查 / 重启 / 停止 / 退出） |
| `启动Uchat.bat` / `稳定运行Uchat.bat` / `停止Uchat.bat` / `保持唤醒.ps1` | ✅ | **排障用**（有窗口、能直接看日志）：`稳定运行Uchat.bat` = 崩溃自动重启 + 阻止休眠 |
| `keystore.p12` / `keystore.pwd` | ✅ | HTTPS 证书与口令（证书是 `*.YOUR-DOMAIN.example.com` 的，见第四节） |
| `turn/` | ✅ | 本地 TURN 中继（随服务自启，UDP 3478） |
| `bot/` `bot2/` | ✅ | 两个 AI 用户的配置与启动器（随服务自启） |
| `java/` | 仅 **full** 包 | slim 包要求目标机已装 Java 17 |
| **`invite.pwd`（注册码）** | ❌ | **必须从旧机器拷过来**，否则新机器**无法注册新账号** |
| **`dsapi.txt`（DeepSeek key）** | ❌ | 想让两个 AI 也在这台机器上跑，就拷过来（或填 `bot\bot.pwd`） |
| `data/`、`logs/` | ❌ | 刻意不带（否则会把旧数据/旧账号带过去） |

> ⚠️ **老包提示**：v2.8.2 之前的包（如 `uchat_2.7.2_*`）里**没有** `静默运行Uchat.*` / `静默监视Uchat.ps1` /
> `静默停止Uchat.ps1` / `_run_uchat_service.cmd` —— 那种包请直接用 `稳定运行Uchat.bat`（功能一样，只是挂着窗口）。

## 二、部署 6 步

1. 把 zip 拷到目标电脑，解压到**纯英文/无空格**路径均可（例：`D:\Uchat`）
2. 从旧机器拷 `invite.pwd`（6 字节）到解压目录的 `uchat\` 根下
   —— 不拷的话：**老账号仍能登录**，但**注册会被拒绝**（设计如此，防止无邀请码被随意注册）
3. （可选）拷 `dsapi.txt` 到 `uchat\` 根下，让两个 AI 也能工作；只用其中一个 AI 也行
4. 双击 **`静默运行Uchat.vbs`**（推荐）；若包里没有这个文件，双击 `稳定运行Uchat.bat`。
   弹窗防火墙时**允许**（HTTPS 8888 + UDP 3478）
5. 浏览器打开 `https://localhost:8888`（首次会因证书域名不符告警 ⇒ 选「继续访问」）
6. 要外网访问：给这台机器配**它自己的** frp 隧道/端口映射（对外端口与旧机器**错开**）

## 三、常见问题

- **注册报「注册码错误」** ⇒ 忘了拷 `invite.pwd`（或没设 `UCHAT_INVITE_CODE` 环境变量）
- **两个 AI 没有回复** ⇒ 缺 DeepSeek key（看 `bot\run*.log`；启动器每 60 秒重试一次，不刷屏）
- **提示端口被占用** ⇒ 已有别的实例在跑（本目录的单实例守卫会拦住）
- **同机开第二个实例** ⇒ 不建议（要改 `--server.port`，且 TURN 的 3478 会冲突）
- **想让现在这个房间容纳更多人** ⇒ 不是靠拷贝：`MAX_CLIENTS = 16` 是代码常量，需要改代码重新打包
  （一个实例 = 一个房间；目前是单房间设计）

## 四、关于证书

包内证书 SAN 为 `*.YOUR-DOMAIN.example.com` / `YOUR-DOMAIN.example.com`（2026-09-23 起为 **Let's Encrypt 通配证书**，
本机每天 03:30 有计划任务自动续期；有效期至 2026-12-22）。
- 用**该域名**访问 ⇒ 无告警（任意 `xxx.YOUR-DOMAIN.example.com` 子域都覆盖）
- 用 **IP / 其它域名**访问 ⇒ 浏览器证书告警（选「继续访问」可正常使用；手机相机/麦克风在部分浏览器会要求受信任证书）
- 想彻底无告警 ⇒ 给目标机**它自己的域名**并替换 `keystore.p12`（用 acme.sh/certbot/lego 申请，再转成 p12，别名保持 `uchat`）

## 五、数据与备份

- 账号与聊天记录都在 `<解压目录>\data\`（`users.dat` 用户表、`room.dat` 聊天历史、`private.dat` 离线私聊；
  听歌房上传的本地音频在 `data/music/`，只保留最近 40 个、单曲 ≤30MB）
- 备份 = 停服后拷走整个 `data\`；重置 = 停服后清空 `data\`（服务重启会重建空房间）
- ⚠️ **不要在服务运行时直接改 `data\users.dat`** —— 内存里的用户表会在保存时整表写回，手改会被覆盖
- ⚠️ `data\*.dat` 一律是 **UTF-8**：用 PowerShell 的 `Get-Content`/`Set-Content` 默认按 GBK 读写会把昵称写坏
  （本机已有字节级工具 `dev\_strip_accounts.py`）

## 六、静默运行怎么开 / 怎么停（v2.8.2 起）

- **开**：双击 `静默运行Uchat.vbs`（隐藏窗口启动 `静默运行Uchat.ps1` 守护 + `静默监视Uchat.ps1` 托盘图标）
- **看状态**：托盘图标颜色；右键菜单可「打开页面 / 日志 / 立即检查 / 重启服务 / 停止 / 退出」；
  详细状态在 `logs\_service_status.json` 与 `logs\_status_monitor.log`
- **停**：双击 `停止Uchat.bat`（它**先停守护再停服务**；直接杀 java 会被守护 3 秒内拉起来）
- **想排障看日志**：改用 `稳定运行Uchat.bat`（有窗口），或看 `logs\server_<时间戳>.txt`
