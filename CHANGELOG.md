## [bot] 2026-09-25 — AI 用户（大肥鱼 / 资料鱼）上下文记忆改善

> 背景：用户发现"上一句说我不吃芹菜，下一句却回你咋知道我不吃芹菜？"——机器人没有上下文/记忆。
> 排查结论：滑动窗口机制其实在，但有两个真缺口：**history 只在内存**（重启即忘）、
> **收不到自己说过的话**（服务端不回声自己的消息，而登录时历史回放里自己的消息被丢弃）；
> 另外原 system 提示里的「历史中已回答的绝不再重复」会诱导模型**否定自己刚说过的话**。

**改动（`bot/chatbot.mjs`，服务端零改动）**
- **历史落盘**：`bot.history.json`（按 `--base` 目录分实例），启动时载入最近 N 条 ⇒ 重启不丢记忆；
- **收下自己的话**：`chat` 帧里昵称等于自己 ⇒ 按 **assistant** 轮记入（登录回放也吃进来）；
- **`sync_result` 补齐的消息并入上下文**（原来只打了一行条数日志）；
- **上下文锚点 + 一致性规则**：system 里显式写「你（<昵称>）最近说过：「…」」「对方最近说：「…」」，
  并要求"与上文一致、**绝不能否认自己刚说过的话**"；
- **去重**：回放/补齐与本地记录重复时跳过（同昵称同内容、最近 6 条内），prompt 不会出现两遍；
- 两个 bot 的 `contextMessages`：大肥鱼 20→**30**、资料鱼 16→**24**；
- 新增 `--dump-prompt`：把最终 messages 落到 `<base>/bot.prompt.json`（便于断言上下文）。

**验证**
- `bun dev/_bot_context_test.js`（mock 模型 + dump-prompt，**9/9 PASS**）：第二问的上下文里确实带上机器人自己的上一句、
  system 含"你最近说过"锚点、历史已落盘、**重启后仍载入历史**且新问题上下文里仍带先前回复。
- **生产真实模型验证**（`bun dev/_bot_real_check.js`，临时账号，完事已注销）：
  - 问"香菜和芹菜哪个好吃" → 「刚不都说了嘛，香菜！…」
  - **记忆探针**"你刚才说你不吃什么？" → 「**芹菜**啊，你自己回头看聊天记录去。」✅ 记得自己的上一句
  - 复现用户那条"不吃芹菜的人品味都很差" → 「品味差就差呗，反正我不吃，你能拿我咋地。」✅ **不再自我否认**
- 两个机器人已重启（守护 3 秒内拉起），日志显示 `已载入历史 N 条`。

## [2.9.11] - 2026-09-25

> 主题：**内存/进程占用排查与页面侧优化**（用户反馈：Chrome 只开 Uchat 就 300+MB、任务管理器 11 项任务）。

### 排查结论（先量化再动手）
- **任务管理器那 11 项主要是服务端进程**：实测 Uchat 相关 **10 个进程 / 716 MB** ——
  java 服务 179 + **3 个 PowerShell 各约 100（守护 + 托盘 + 一个遗留的测试进程）** + 3 个 bun（TURN + 两个 AI）各 62 + 3 个 cmd 包装 8.5。
  其中**漫画阅览器测试残留**（1 个 PowerShell + 4 个 bun ≈ **432 MB**）已清掉 ⇒ 现在 **8 个进程 / 548 MB**。
- **Chrome 那 300+MB 主要是 Chrome 自己的多进程基线**（浏览器/GPU/网络/存储/渲染/工具进程，一个空白标签页也差不多），
  实测**我们页面自身的 JS 堆只有 3.6 MB、DOM 810 个节点**（JS 共约 395 KB）。页面能显著吃内存的只有**聊天记录里的图片/音视频解码缓冲** ⇒ 见下面的优化。

### 页面侧优化（三处，可量化）
1. **聊天图片加 `loading="lazy" decoding="async"`** —— 视口外的图片不再解码（图片位图是页面内存的最大头）。
2. **视频消息不再一渲染就探测时长**：原先每条视频消息都会建一个隐藏 `<video preload=metadata>`（= 一个解码器 + 一次请求）；
   改成**滚到视口附近（提前 400px）才探测** ⇒ 实测**12 条视口外视频消息的探测次数 12 → 0**。
3. **媒体滚出视口较远自动释放**：`<audio>`/`<video>` 的 `src` 被清掉并 `load()`（解码器与缓冲随之回收），
   滚回来会重新加载 ⇒ 实测音频被推出视口后 `src=released, paused=true` ✓。
   三者共用同一个 `IntersectionObserver`（`watchNearViewport` / `_ensureMediaObserver`）。

### 验证
`bun dev/_mem_opt_test.js`（临时实例 + 真 Chrome）：图片 `loading=lazy` ✓、12 条视口外视频消息**探测 0 次** ✓、
滚到附近才开始探测 ✓、音频滚远后 `src` 被释放 ✓、页面指标 JS 堆 3.6 MB / 810 节点 ✓。

### 未做（成本/收益不划算，等你拍板）
- **把托盘进程并进守护进程**可再省约 108 MB、少 1 个进程；**机器人改由守护直接拉起**（去掉 2 个 cmd 包装）再省约 17 MB。
  收益是 100 多 MB（机器 31 GB），代价是动"保命脚本"（守护/托盘/停止链路），**有服务中断风险** ⇒ 默认不动。
- java 的 `-Xmx768m` 可降到 512m（RSS 179 MB），收益很小。

## [2.9.10] - 2026-09-25

> 主题：**清理"根本不使用"的依赖与死代码**（用户提问）。结论：**Maven 依赖和前端 vendor 库都没有多余的**，
> 真正能清的是一批**死代码**（约 150 行），已清掉并用冒烟测试验证聊天链路无回归。

### 核对结论（有证据）
- **Maven 依赖**：`pom.xml` 只声明 4 个 —— `spring-boot-starter-websocket`、`spring-boot-starter-web`、
  `gson 2.10.1`、`spring-boot-starter-test`（test）。`mvn dependency:analyze` 报的
  「Unused declared: spring-boot-starter-*」是**已知误报** —— starter 是**空的聚合 POM**（不含任何类），
  字节码自然"引用不到"它；删掉它会破坏 Spring Boot 自动配置。判据：源码里 `com.google.gson` 出现 19 处、
  `org.springframework.web.socket` 11 处、`org.springframework.web.bind` 11 处、`org.junit` 20 处 ⇒ **全都在用**。
  打出来的 jar 里也只有 **31 个库**，已经是精简状态。
- **前端 vendor**：`highlight` / `katex`（含 contrib）/ `marked` 的 6 个 js+css 全部被页面引用 ✓，
  KaTeX 的 20 个 woff2 字体由 `katex.min.css` 按需引用 ✓ ⇒ 无需删除。

### 清掉的死代码
- `chat.js`：**整套「HTTP 兜底聊天」** —— `connectWebSocketGuest` / `connectHttpChatGuest` / `connectHttpChat` /
  `httpSendMessage` / `httpPoll`（它们打的是 `/api/chat/auth|send|poll`，而 `ChatHttpController` 已在 v2.9.7 删除 ⇒ 必然 404）；
  连同 3 处 `httpMode` 分支、4 个已无人使用的变量、以及没有任何调用点的 `checkFileStale()`。
  文件从 68,830 → **62,729 字节（−6,101，约 150 行）**。
- `Room.java`：删掉 `isShutdown()`（全项目 0 个调用点）。

### 验证
`bun dev/_smoke_test.js`（临时实例 + 真 Chrome）→ 真实链路全过：
页面 WS 登录 ✓、**页面发消息 → 对端收到** ✓、**对端发消息 → 页面渲染** ✓、带 token 上传 200 ✓、文件消息渲染 ✓、**页面 0 个 JS 异常** ✓。

## [2.9.9] - 2026-09-25

> 主题：音频播放卡片的暗色适配与标题排版（用户反馈）。验收 `bun dev/_audio_theme_test.js` → **7/7 PASS**。

### ① 播放/倍速按钮没适配暗色（一直灰白）
- 根因：v2.9.8 写的样式里用了 `var(--bg-2, #f3f4f6)`，而主题里**没有 `--bg-2` 这个变量**
  ⇒ 两套主题都落到亮色兜底值 `#f3f4f6`。卡片背景同样用了不存在的 `--panel-bg`。
- 修：改用 `light.css` / `dark.css` 里真实存在的变量 ——
  按钮 `--btn-bg`（hover `--btn-bg-hover`）+ 文字 `--text-primary` + 边框 `--border-color`；
  卡片背景 `--bg-secondary`、边框 `--border-light`；进度条/音量条 `accent-color: var(--accent)`；下载链接 `--text-link`。
- 实测（同一张卡片切主题量计算样式）：按钮背景亮度 **亮 224 → 暗 64**、按钮文字色 `#333` → `#d8dce6`、卡片背景 **255 → 49** ✓。

### ② 歌名与控件挤在一行导致长标题显示不全
- 原布局：歌名是第二行里的 `flex:1` + `text-overflow: ellipsis`，和倍速/音量/下载抢空间 ⇒ 长标题被省略号截断。
- 修：歌名**独占第三行**（`width:100%`、`white-space: normal`、`overflow-wrap: anywhere`），卡片上限由 340px 放宽到 `min(100%, 420px)`。
- 实测：57 字符标题**不再截断**（`scrollWidth ≤ clientWidth`）、`capOwnRow=true`（确实在倍速按钮下方）、自动换行到 30px（两行）✓。

## [2.9.8] - 2026-09-25

> 主题：音频/视频播放器的交互修复（用户三条反馈）。验收 `bun dev/_player_ui_test.js` → **13/13 PASS**。

### ① 音频改成**内联自绘播放器**（不再用原生 `<audio controls>`）
- 原生控件把 **音量 / 倍速 / 下载** 都塞在浏览器自己的弹出菜单里：调完音量进度条会"收起"、要再点一下才回来（极易误触）；
  消息靠屏幕上方时那个弹出层还会被页面顶部/标签栏遮住。
- 现在音频卡片里平铺一整套控件：**▶/⏸ · 进度条 · `当前/总时长` · 倍速（1→1.25→1.5→2→0.5） · 音量滑杆 · ⬇ 下载**，
  全部在同一行/两行内，没有弹出层 ⇒ 不误触、不被遮挡。
- 实测：调音量后进度条仍可见（`seekVisible=true`）✓；倍速点一下变 1.25× 且标签同步 ✓；进度条拖到中点 `currentTime≈51%` ✓；暂停图标回到 ▶ ✓。
- 文件总览里的音频也用同一个播放器。

### ② 视频按"浏览器能否内嵌播放"分流
- **可播**（mp4 / webm / m4v / ogv / mov）→ 缩略图（异步读时长）+ 点一下就地展开 `<video controls playsinline>` + **⬇ 下载**按钮。
- **不可播**（avi / mkv / wmv / flv / mpg / 3gp / rmvb）→ 卡片明确写
  「🎬 文件名 · 格式 .avi · 大小 · **浏览器无法内嵌播放**（avi/mkv/wmv 等需用本地播放器）」+ **⬇ 下载原文件**，
  不再挂一个永远转不出预览信息的播放器（用户上传 avi 遇到的正是这个）。
- 实测：avi → 出说明卡 + 下载链接、无 `<video>` ✓；mp4 → 缩略图点开即 `<video>` 且带下载 ✓。

### ③ 服务端上传回执的 type 细分
- `/api/files/upload` 原来只返回 `image` / `file`（视频被标成 `file`）；现在按扩展名（辅以 MIME）返回
  **`image` / `video` / `audio` / `file`** ⇒ 前端可直接据此选控件。实测 .wav→audio、.avi/.mp4→video、.txt→file ✓。

## [2.9.7] - 2026-09-25

> 主题（用户三项要求）：① 修掉遗留的三个安全问题 ② 聊天室文件占用超 50GB 时禁止上传并提示联系管理员
> ③ 音频/视频文件在聊天室**直接播放**（免下载）。验收：`bun dev/_files_media_test.js` → **15/15 PASS**。

### ① 三个遗留问题
- **`/api/prefs/*` 与 `/api/pomodoro/*` 加 token 鉴权**：原先只知道昵称就能读/写别人的偏好与番茄钟统计。
  实测：无 token → `{"error":"未登录或凭证过期"}`（pomodoro 为 `{"ok":false,...}`）；带 token → 正常。
  前端 `app.js` 四处调用相应补上 `nickname+token`。
- **下线早期 HTTP 聊天接口 `ChatHttpController`**（`/api/auth`、`/api/send`、`/api/poll`、`/api/logout`）：
  已被 WebSocket 取代、无鉴权、`send` 还能**冒充任意昵称**发消息；前端与测试均已无人调用 ⇒ 直接删类。
- **CORS 逗号列表**：`allowedOriginPatterns(allowedOrigins)` 不按逗号拆分 ⇒ 已改为 `split("\\s*,\\s*")`（此前一轮已修，本轮确认）。

### ② 聊天室存储配额（默认 50GB）
- 新增 `StorageQuota` 组件：统计 **共享文件（`data/users/shared/files`）+ 听歌房音频（`data/music`）** 总占用，
  上限由 **`chat.files.max-total-gb`（默认 50）** 控制；超过即**禁止上传**，返回 **507** 并提示：
  「服务器存储空间已满：聊天室文件已占用 X GB（上限 50 GB），暂时无法上传。请联系管理员 admin 清理空间。」
  （管理员名字取自 `chat.roles.admin` 的第一个）
- 同时**取消** v2.9.5 的「512MB 静默回收」——按用户要求改为"禁止上传 + 提醒"，不再悄悄删别人的文件。
- 文件与听歌房两条上传路径都受此限制；占用值 10 秒缓存，避免每次上传都全目录扫描。

### ③ 音频 / 视频在聊天室直接播放
- **服务端**：`/api/files/preview` 与 `/api/files/download` 支持 **HTTP Range**（`206` + `Content-Range` + `Accept-Ranges`，
  支持 `bytes=a-b` / `bytes=a-` / `bytes=-n` 与 HEAD），文件用 `RandomAccessFile` 按区间输出，客户端提前断开（关页面/切歌）不再报错。
  没有 Range 时 `<audio>/<video>` 无法拖动进度、部分浏览器干脆不播。
- **聊天消息里**：音频 → 内联 `<audio controls preload=metadata>`（`🎵 文件名 (大小)`）；
  视频 → 缩略图**点一下就地展开** `<video controls playsinline>`（再点收起，播完自动收回）。
- **文件总览**：音频/视频行各自带「▶ 播放」按钮，就地展开播放器（再点收起），图片仍走灯箱。
- 实测（临时实例 + 真 Chrome）：音频消息渲染出 `<audio controls>` ✓、视频卡点开出 `<video controls>` ✓、
  文件总览点播放出播放器 ✓、**音频真能播**（`readyState=4`、时长 30s、进度 1.46s）✓。

## [2.9.6] - 2026-09-25

> 主题：上传接口的畸形/超大请求不再 500（顺手把 v2.9.5 的鉴权路径补严）。

- 实测问题：**未带 multipart 的上传请求**（如 `POST /api/files/upload` 空体）会返回 **500 + 满屏堆栈**
  —— 因为 `@RequestParam MultipartFile file` 在**方法体之前**绑定，鉴权代码根本轮不到执行 ⇒ 500 刷日志。
- 修：`file` 参数改 `required = false`，先鉴权再判空 ⇒ 未登录一律 **403**、缺文件 **400**；
  另加 `@ExceptionHandler`：超过 100MB 的上传返回 **413**（明确文案），multipart 解析失败返回 **400**。
- 线上复查：`GET /api/files/list` → **403**；`POST /api/files/upload`（无 token 空体）→ **403**；`GET` → **405**（都不再 500）。

## [2.9.5] - 2026-09-25

> 主题：**服务器崩溃风险排查与加固**（用户提问："还有会导致服务器崩溃的 bug 吗？着重检查早期版本写的逻辑"）。

方法：静态审计（早期 9/13 未改动的文件优先）+ **对抗性探针**（`bun dev/_server_hardening_probe.js`，独立临时实例，不碰 8888）。
确认并修掉 **2 个崩溃级**问题 + 3 处加固。

### 🔴 崩溃级 ① `/api/files/upload` 无鉴权、无配额 ⇒ 磁盘可被灌满
- 实测：**未登录**上传 20MB → `HTTP 200`（早期版本 upload 没有 token 校验）；而 `spring.servlet.multipart.max-file-size=100MB`
  ⇒ 任何人都能反复上传把盘塞满 ⇒ 服务写不进 `room.dat` / 日志，等于宕机。
- 修：upload 加 token 鉴权（与 download/preview 一致）⇒ 实测未登录 **403**、带 token **200**；
  新增**共享目录总配额 512MB**（超配额按最旧优先回收、跳过 `.tmp`）；前端上传请求补上 `nickname+token`。

### 🔴 崩溃级 ② `/api/files/list` 的 `Files.list()` 流未关闭 ⇒ 句柄泄漏
- 实测：**300 次**调用后 java 进程句柄 **612 → 908（+296，≈1 个/次）**；被刷新/被刷 ⇒ 句柄耗尽 ⇒ **新连接全部失败**。
- 修：改用 `try (Stream<Path> s = Files.list(...))` 显式关闭 + 加 token 鉴权
  ⇒ 实测 300 次带 token 调用后句柄 **609 → 606（增量 -3）**，不再泄漏。

### 加固
- `download` / `preview` 加 `safeResolve()`（`normalize()` + 前缀校验），不再依赖容器层挡 `..\`（实测 4 种穿越写法本就取不到文件，属纵深防御）；
- `Room.saveToFile()` **加 `synchronized`**：saver 线程（每 10 秒）与消息线程（`addHistory` 的节流保存）会同时写同一个 `room.dat.tmp`，可能互相踩坏数据；
- `FileController.songName` 对远端页面改为 `readNBytes(256KB)`，不再 `readAllBytes()` 无上限。

### 压力测试（都在独立临时实例上）
| 项 | 结果 |
|---|---|
| 无鉴权上传 20MB | **403 拒绝** ✓（修前 200） |
| 带 token 上传 | 200 ✓ |
| 无鉴权文件列表 | 403 ✓ |
| 反斜杠路径穿越（4 种写法） | 未取到任何文件 ✓ |
| 300 次带 token 列表 | 句柄 609 → 606 ✓（修前 +296） |
| WS 畸形帧轰炸（坏 JSON / null / 缺字段 / 错类型 / 200KB 超大载荷） | 服务健康 ✓、正常消息仍可收发 ✓（异常被逐条吞掉，日志 120 行，服务不崩） |
| 200 次连接-登录-断开风暴 | 服务健康 ✓、句柄回落 ✓ |

### 已知但未修（都不是崩溃级，按你的取舍）
- `/api/prefs/*`、`/api/pomodoro/*` **无 token 校验**（知道昵称就能读写他人偏好/番茄钟统计）——要改需前后端一起改；
- HTTP 版聊天接口 `/api/auth`、`/api/send`、`/api/poll`（早期遗留、已被 WebSocket 取代）无鉴权，`send` 还能冒充任意昵称，建议下线；
- `chat.allowed-origins` 逗号列表实际失效（`allowedOriginPatterns` 不按逗号拆分），只在跨源场景才有感。

## [2.9.4] - 2026-09-25

> 主题：修「每次版本更新后，之前隐藏的工具栏按钮又冒出来，要手动勾选再取消」（用户反馈）。

### 🔴 根因
设置面板里那组「工具栏按钮」勾选框是**页面初始化（登录前）**就构建的，把当时的**空昵称**烘进了存储键：
写入变成 `uchat-tb--emoji`（昵称位为空）；而登录后 `applyToolbarVisibility()` 按**真实昵称**读
`uchat-tb-<昵称>-emoji` ⇒ 读不到 `hidden` ⇒ 把按钮**重新显示出来**。
所以「手动勾选再取消」只在本会话有效，**重新打开页面（用户感知为"版本更新后"）就失效**。

实测（`bun dev/_toolbar_pref_test.js`）：勾掉「表情 / 骰子」后，localStorage 里写的是
`{"uchat-tb--emoji":"hidden","uchat-tb--dice":"hidden"}`（脏键），刷新 + 重登后按钮 `display:flex`（又出现）。

### 修法
1. **键一律在读写时按当前昵称计算**（`_tbKey()` 兜底 `anon`），不再把 key 烘进 DOM；
2. 统一按钮表 `_TB_BTNS`；`applyToolbarVisibility()` 一次同时同步「工具栏显隐」+「设置里的勾选状态」；
3. **迁移历史脏键**：`uchat-tb--<name>` → `uchat-tb-<昵称>-<name>`（目标不存在时迁移）
   ⇒ 更新后你**之前的选择会自动恢复**，不用重新勾一遍；
4. 隐藏改用 CSS 类 **`.tb-hidden { display: none !important; }`**（别处再改 inline display 也盖不掉），保留 inline 兼容；
5. 登录后按 200 / 800 / 2000 / 4000 ms **多点重复应用**（幂等），覆盖登录页、注册失败转登录、自动登录等所有路径。

### 验收
`bun dev/_toolbar_pref_test.js`：设置里取消勾选「表情」「骰子」→ **刷新页面并重新登录** →
两个按钮仍隐藏（`display:none`）、设置里的勾选框仍未勾选、没动过的「文件」仍可见、
存储键都带昵称且无脏键、页面无 JS 异常 ⇒ **全部 PASS**。

## [2.9.3] - 2026-09-25

> 主题：修屏幕共享「观看人数」的三个真问题 + 画质/帧率选项调整（用户实测反馈）。

### 🔴 人数统计（三条都修了）
1. **一个人共享却显示 2 人**：根因是"页面重新可见时自动重发观看请求"没排除**我自己就是共享者** ⇒
   共享者请求观看自己的共享 → 服务端把他记成观看者、他给自己建了一条 PC（`have-local-offer`）⇒ 人数 +1。
   四层防护：① 客户端重发前要求 `currentScreenSharer !== nickname`；② 人数统计过滤掉自己；
   ③ 服务端 `handleScreenWatch` 忽略自请求；④ 下发名单时过滤掉共享者自己。
2. **观看者退出后人数不降 / 反而 +1**：原来点 ✕ **只是本地关掉、PC 还留着**，再点观看时因为"已有 PC"
   而不发请求（黑屏 + 人数对不上）；而"页面重新可见自动重发"又会让人数跳回来。
   现在 ✕ 会关掉自己的 PC 并置 `screenViewerDismissed`（不再自动重发），同时发 `screen_unwatch` 通知服务端。
3. **人数改为服务端权威**：服务端维护 `sharer → 观看者集合`（watch / unwatch / 断线三种情况都维护），
   变化即下发 `screen_viewers`；客户端以它为准（并区分"还没下发"与"服务端说 0 人"），本地 PC 表只作兜底；
   观看者断线时服务端会把他从所有名单里摘掉。

### 选项调整（用户实测：90/120fps 带宽根本不够）
- 帧率：**24 / 36 / 48 / 60**（去掉 15/30/90/120）；老设置自动迁移（15→24、30→36、90→60、120→60）。
- 画质：**480p / 720p / 1080p / 2K（2560×1440）**（共享条与设置页两处同步）。
- 每路码率口径不变（只跟帧率有关）：24/36/48fps → 1.5 Mbps，60fps → 3 Mbps，估算行如实显示。

### 验收（真 Chrome + CDP，两页，canvas 冒充屏幕采集 + 真实 WebRTC）
`bun dev/_screen_count_probe.js`：一个人共享 → `👥 1 人观看`；观看者点 ✕ → `0 人观看`（服务端名单同步清空）；
再次观看 → `1 人观看`（**不再 +1**）；观看者直接关标签断线 → `0 人观看`；
**共享者自己的昵称从不出现**在本地 PC 表 / 服务端名单里。
选项检查：`fps=[24,36,48,60]`、`quality=[480p,720p,1080p,2k]`（共享条与设置页一致）；老值 90/120→60、15→24。

## [2.9.2] - 2026-09-25

> 主题：屏幕共享的两个可观测性改进（用户要求）—— **共享者看到上行占用估算**、**观看者看到实时网络延迟**。

### 共享者：上行占用估算（`#screen-share-stats`）
- 共享条上新增一行：`👥 3 人观看 · 上行 ≈ 4.5 Mbps（每路 1.50）`；颜色随占用比变化
  （<60% 绿 / 60–85% 黄 / ≥85% 红），≥85% 时弹一次「已接近你的出口 —— 建议降到 720p/15fps，或让部分人停止观看」。
- 口径：**观看人数 × 每路码率上限**。每路码率只跟**帧率**有关（15/30fps→1.5 Mbps、60fps→3 Mbps、≥90fps→5 Mbps，
  与 480p/720p/1080p 无关 —— 清晰度只影响采集分辨率）；共享系统音频时每路再加 ~128 kbps。
- 出口估值默认 **30 Mbps**（本机实测 ≈29），可用 `localStorage.setItem('screen-uplink-mbps','50')` 改成实际值。
- 每 5 秒刷新一次；观看者进出 / 切帧率或清晰度会立即更新。

### 观看者：实时网络延迟（`#screen-viewer-stats`）
- 观看浮窗上新增：`● 良好 · 延迟 42ms · 30fps · 丢包 0.3%`，颜色分档（<150ms 且丢包<2% 绿 / <300ms 黄 / 其余红），
  悬停看完整详情（RTT / 丢包 / 解码帧率 / 画面分辨率）。2 秒刷新。
- 算法与语音通话的质量徽标同一套：nominated candidate-pair 的 RTT + **增量**丢包率。
- 对屏幕共享特别有意义：共享设了 `maintain-resolution` ⇒ 弱网**先掉帧不降分辨率**，
  所以"延迟高/帧率掉"就是卡顿的直接证据；也能更快分清是谁的问题（**所有人同时掉** = 共享者上行/CPU；**只你掉** = 你的下行或走了中继）。

### 配套修复
- 观看者关闭画面时**什么都不发**，共享者的估算会一直把走掉的人算进去（直到 ICE 超时）。
  现在观看者关闭会发 `screen_unwatch`，服务端转告共享者 `screen_viewer_left`，共享者立刻释放那一路；
  估算里对 `failed/closed` 的连接也做了自清洁。

### 验收
`bun dev/_screen_stats_test.js`（真 Chrome + CDP，两页：**canvas 冒充屏幕采集**、其余走真实 WebRTC）→ **13/13 PASS**：
`1 人观看 ≈ 1.5 Mbps` → 切 60fps 变 `≈ 3.0 Mbps（每路 3.00）` → 出口估值改 2 Mbps 后变红并弹告警 →
观看者点 ✕ 后回落 `0 人观看`；观看者一侧显示 `● 良好 · 延迟 1ms · 1fps` 且持续刷新。

## [2.9.1] - 2026-09-24

> 主题：**修「听歌全是滋滋啦啦杂音」**（用户反馈；v2.9.0 引入的严重缺陷）+ 房间容量显示/文档订正。

### 🔴 杂音根因（已定位到具体机制并修掉）
v2.9.0 为了让"缓存命中时也能拿到时长"，把 `<audio>` 的事件顺序改成先挂载再设 `src`，
顺手写了 `player.oncanplay = player.onloadedmetadata;` —— 而这个 handler 结尾是**硬 seek**（对齐房间进度）。
**`canplay` 在每次 seek 之后都会再次触发**，于是形成 `canplay → 硬 seek → canplay → …` 的死循环：
音频被切成碎片、永远播不完整 ⇒ 听到的就是**滋滋啦啦的杂音**（用链接或本地音频都一样，因为都走这条路径）。

实测（真实客户端流程 + AnalyserNode 测**实际输出波形**，听众端采样 15 秒）：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| `seeking` 次数 | **53694**（≈3500 次/秒） | **0** |
| `readyState` | 在 1 / 4 之间反复跳 | 稳定 4 |
| 输出 RMS（音乐应 ~0.03–0.15） | 0.0018（近乎静音/杂音） | 0.04–0.08（正常音乐） |
| 过零率 ZCR | 0.0036 | 0.03–0.12（音乐特征） |
| 与房间进度的偏差 | — | 0.08 秒内 |

**改法**：① 删掉 `oncanplay` 别名；② 「首次对齐」每首歌只做一次（`mrAlignedKey`），之后只做容差校正；
③ seek 加节流（≥1.5 秒一次）且要求 `readyState≥2`、时长已知、目标 >0.5 秒；④ `mrStop` 不再写 `player.src=''`
（那会触发一次 `error(code 4)` 并误报"这首歌放不出来"），改为摘掉属性 + 清事件。

### 其它修复
- **通话房间卡片"人数分母"在切换类型时不同步**：语音切视频后标题变了、右下角却仍显示 `0/12 人`（应为 `0/8 人`）。
  现在 `updateRoomCard` 记录各房间人数，`renderRoomCards` 切换类型时连分母一起重画。
- 文档订正：`DEPLOY.md` 排障表「每间最多 4 人」→「语音房 12/6 人、视频房 8/6 人（v2.9.0 起）」；
  `DOCS.md` 两张特性表「独立 2 间各 8 人」→「独立 2 间（12 / 6 人）」；`ChatWebSocketHandler` 里过时注释一并更新。

### 验收
- `bun dev/_music_flow_probe.js`（真实客户端流程 + AnalyserNode）→ 上表读数；
- `bun dev/_music_sync_test.js` → **18/18 PASS**（新增回归守护「听众端 6 秒内 seek ≤5 次」实测 0 次；
  真同步改用与解码无关的"期望位置"判据：差 0.00 秒）；
- `bun dev/_call_ui_test.js` → **11/11 PASS**。

## [2.9.0] - 2026-09-24

> 主题：**听歌房（本地音频 + 真同步 + 版权兜底）** 与 **通话房间容量 / 换房**。

### 听歌房
- **新增「📁 本地音频」**：选本地 mp3/ogg/wav/m4a/flac 等（≤30MB）上传到服务器（`data/music/`），
  房间内所有人用**同一个音源**播放（上传 `POST /api/music/upload`；播放 `/music/<file>` 走 Spring 静态映射，
  以取得 **HTTP Range** 支持 —— 没有它 `<audio>` 无法 seek 到同步位置）。目录自动只保留最近 40 个文件。
- **保留网易云链接**，但**版权/外链失效必须可见**：点歌人端提示「⚠ 这首歌放不出来：…（可能受版权限制）」并上报服务端
  → 服务端广播「⚠ xx 的曲目无法播放…已自动跳过」→ 自动换下一首；另有 90 秒兜底推进（连上报都没有时也会走）。
  加载超时 20 秒（手机慢网也够）。
- **真同步**：状态里带 `startedAt`（服务器起点时间）与 `serverTime`；客户端算 `(服务器现在 − startedAt)` 并 seek，
  每 2 秒校正（偏差 >0.8 秒拉回）⇒ 中途加入也对齐；点歌人上报 `music_room_meta`（时长）供服务端精确排下一轮。
- 面板重做：曲目名 / 只读进度条 / `⟳` 强制对齐 / 同步状态 / 手势兜底按钮「▶ 点这里开始同步播放」。
- **修**：自己退出听歌房后本地仍在继续放歌（旧实现只看"房间是否为空"）。
- **修**：`<audio>` 事件改为**先挂载再设 src** —— 命中缓存时 `loadedmetadata` 可能早于挂载，导致点歌人端拿不到时长、
  不 seek、也不真正播放（听众端反而正常）。

### 通话房间
- 容量按房间区分：**语音房 12 / 6 人、视频房 8 / 6 人**（原来是统一 `callRoomMax=6`）；由 `call_status.caps` 下发给客户端
  （卡片原来写死 `0/4 人`）。
- **支持通话中直接换房**：旧版"通话中再点呼叫键"是**直接挂断**；现在打开「🔀 换房间」，语音⇄视频、1 号房⇄2 号房都能切
  （服务端立即清掉旧房间），并新增选择器里的「退出通话」按钮。
- **修**：跨类型换房时标题里的"当前房间"错显示成目标类型（新增 `callRoomIsVoice` 记录**实际所在**房间）。
- 引导文案与按钮 title 同步更新为「通话 / 换房间」。

### 验收（真 Chrome + CDP + 独立实例，不碰 8888）
| 套件 | 结果 | 关键读数 |
|---|---|---|
| `bun dev/_rooms_test.js`（房间容量/换房，纯 WS） | **16/16 PASS** | 12/6/8/6 全部生效且提示明确；caps 正确；换房后旧房间立即清空 |
| `bun dev/_music_sync_test.js`（听歌，两个浏览器页） | **17/17 PASS** | 真同步差 0.75s；**中途加入差 0.01s**；时长 30s 上报成功；版权失败 → 明确提示 + 自动跳过（DJ 顺延） |
| `bun dev/_call_ui_test.js`（通话 UI/换房，真浏览器 + 假麦克风） | **11/11 PASS** | 卡片 12/6、8/6；换房标题「🔀 换房间（当前：语音房间 1）」；语音房2→视频房1 成功；退出后四房间归零 |

## [2.8.5] - 2026-09-24

> 主题：**@ 补全**（PC 与手机通用，仅在线用户）+ 手机端**取消语音测试**。

### 新增：@ 补全（双端通用，**只列在线用户**）
- 在输入框里打 `@`（或 `@` + 前缀）→ 输入框上方弹出候选面板：**仅在线用户**、排除自己；
  前缀优先、其次包含匹配；带角色标记（👑 管理员 / 🤖 机器人）；最多 8 条；没有匹配时提示「没有匹配的在线用户」。
- 操作：`↑`/`↓` 选择、`Enter`/`Tab` 接受、`Esc` 关闭；鼠标点击与**手机触摸**都可用
  （手机端内边距 12/14、字号 16px，触摸目标够大）。
- 接受后把 `@前缀` 替换成 `@昵称 `，光标移到其后，继续打字即可。
- 🔴 **实现要点**：补全的 `keydown` 挂在**捕获阶段**，抢在既有的「Enter 发送」监听器之前 ——
  否则面板开着按 Enter 会先把消息发出去。实测（桌面回归 D11）：Enter 接受补全时消息数 1 → 1，**没有误发**。
- 点选用 `mousedown`/`touchstart` + `preventDefault`，避免输入框先失焦、面板被收起导致点不中。
- 输入框失焦 / 发送后会收起；在线名单变化时面板按新名单刷新（正在 @ 的人下线会被剔除）。

### 手机端：取消「🎤 语音测试」
- 按要求，手机设置页不再显示语音测试块（音量 / 监听 / 降噪）；**桌面端照旧保留**。
- 手机设置页现在隐藏 **7 项**：消息字体 / 麦克风 / **🎤 语音测试** / 摄像头 / 屏幕共享默认清晰度 / 屏幕共享帧率 / 发送习惯。

### 验收（真 Chrome + CDP + 独立 8899 实例，不碰 8888）
- **手机** `bun dev\_mobile_audit.js --tag mention`：打 `@` → 候选 = 在线用户、面板位于输入框上方且在视口内
  （rect 10,732,278,58）；前缀过滤正常；**触摸点选** → 输入框 `@昵称 `、光标 21（末尾）、面板收起、焦点回到输入框；
  无匹配 → 提示「没有匹配的在线用户」；语音测试块 `display:none`。
- **桌面** `bun dev\_desktop_regression.js` → **14/14 PASS**
  （新增 D8 弹出补全 / D9 Esc 收起 / D10 Enter 接受且光标到位 / D11 不误发消息 / D12 桌面语音测试照旧显示 h=22）。

## [2.8.4] - 2026-09-24

> 主题：2.8.3 的收尾 —— **桌面回归抓出的一处自伤** + 骰子"点了没反应"的体感问题。

### 修复
- 🔴 **手机版的行内 `@` / `🔒` 按钮在桌面上也冒出来了**（2.8.3 引入的回归）：
  那条"默认隐藏"的 `.user-row-actions { display: none; … }` 被我写进了**手机媒体查询里**，
  桌面端因此没有任何规则，而 `.user-item` 是 flex 容器会把 `span` 块化 ⇒ 桌面上每行多出两个按钮。
  现在默认隐藏放回**基础 CSS**，媒体查询只负责 `display: flex`。
  （这条是新增的**桌面回归**脚本抓到的：`dev\_desktop_regression.js` 的 D1 项 FAIL → 修 → **9/9 PASS**。）
- **骰子点了"没反应"**：动画 12 帧 × 80ms + 400ms 延迟 = **1.36 s** 才把结果发出去，
  手机上体感就是"这个按钮是坏的"（我做体检时自己都误判成死按钮）⇒ 压到 6 帧 × 60ms + 200ms ≈ **0.56 s**。

### 验收（两份，都是真 Chrome + CDP + 独立 8899 实例，不碰 8888）
- **手机 390×844 + 触摸**：`bun dev\_mobile_audit.js --tag verify3`
  - 工具栏**逐按钮击打**（轮询到有变化为止）：在线列表 / 表情 / **骰子** / 视频通话 / 语音通话 / 切换主题 / 清屏 **全部有效果**；
    Markdown 工具条 8 个按钮全部有效果 ⇒ **手机工具栏没有死按钮**（"不存在的选项"就是设置页里那 4 个勾选项，2.8.3 已删）
  - 抽屉 x=90 可开 · 菜单在最上层 · @提及光标 21 · 私聊整屏 390×844 · 未读角标 1
- **桌面 1400×900 + 鼠标**：`bun dev\_desktop_regression.js` → **9/9 PASS**
  （行内按钮被隐藏 / 手机隐藏的 6 项照旧显示 / 「工具栏按钮」仍是 6 项 / 工具栏 16 个按钮都在 /
  用户菜单三项可点 / @提及正常 / 私聊仍是右下角 360×420 小窗）

### 三条教训（写进证据，避免重犯）
1. 探针**不能固定等一个短时间**就断言"没反应"：动画类操作必须**轮询到变化为止**（骰子假阴性就是这么来的）。
2. 探针**关弹窗不能只点关闭按钮**：卡片比视口高时按钮在滚动区外，点击落空、弹窗一直开着把后续检查全挡掉（假 FAIL）。
3. 我插入 CSS 时用了 LF，导致 `index.html` 变成混排换行（1129 CRLF + 44 LF）⇒ 已统一成 CRLF。

## [2.8.3] - 2026-09-24

> 主题：**手机版功能适配**（用户反馈：手机上 @提及 / 私聊 用不了；设置页一堆无用项；工具栏按钮里有"不存在的选项"）。
> 用真机尺寸的无头 Chrome（CDP，390×844 + 触摸模拟）把手机端逐项量了一遍：`dev\_mobile_audit.js`，
> 前后读数 `logs\_mobile_before.json` / `logs\_mobile_after*.json`，截图 `logs\_mobile_before|after*/`，对比图 `logs\_mobile_对比.png`。

### 修复（只在手机上暴露的真问题）
- **点用户后弹出的菜单被用户抽屉盖住** ⇒ 手机上「🔒 私聊 / @ 提及」根本点不到（这正是不好用的根因）。
  实测读数：菜单 `z-index=500` < 抽屉 620 / 遮罩 610，菜单中心最上层元素是 `user-list`、`menuIsOnTop=false`。
  现在：手机端点用户**先自动收起抽屉**，菜单提到 `z-index:700`（修后读数 `menuIsOnTop=true`、抽屉已收起）。
- **关掉私聊窗口后再收到该人的消息不显示未读角标**：`privateChatTarget` 在关窗后仍保留上一次的目标，
  被当成"正在看"⇒ 不计数。现在要求窗口**确实可见**才算已读（实测：角标 `null` → `1`，打开后清零）。
- **设置卡比手机屏幕高**，「关闭」按钮要滚动才够得着 ⇒ 卡片占满可用高度、按钮行吸底。

### 手机端新增
- **用户列表行内直接给两个按钮**：`@`（提及）与 `🔒`（私聊，带未读角标）—— 一击直达，不必先弹菜单；
  行高 35 → 46px 起（触摸目标）。**桌面端这两个按钮被 CSS 隐藏，行为完全不变**。
- **@ 提及**：插入 `@昵称 ` 后把**光标移到末尾**并聚焦（手机上会拉起键盘），同时收起抽屉。
- **私聊窗口在手机上整屏**（原 360×420 固定右下角，键盘一弹输入框就被顶出屏外）；输入行留 `safe-area-inset-bottom`。
- 手机端收到私聊**不自动弹窗**（整屏窗口会把正在看的聊天顶掉），靠未读角标 + 提示；点行内 🔒 进入。

### 手机端精简
- 设置页隐藏 6 项：**消息字体 / 麦克风 / 摄像头 / 屏幕共享默认清晰度 / 屏幕共享帧率 / 发送习惯**
  （手机浏览器没有 `getDisplayMedia`；手机是点「发送」按钮；设备选择基本由系统接管）。
- 「工具栏按钮」勾选项在手机上**只列出手机上真的存在的按钮**（表情 / 骰子）。
  原来列出 文件 / 环境音 / 番茄钟 / 随机决策 四个 —— 勾了没有任何东西出现，就是用户说的"不存在的选项"。
- 上下文菜单在手机上只留 私聊 / @提及 / 静音（引用 / 复制 / 撤回 隐藏），行高加大到 11px padding / 15px 字号。
- 输入控件（主输入框 / 私聊输入框 / 登录框）字号统一 ≥16px —— iOS 上小于 16px 聚焦会自动放大整页。

### 验收读数（before → after）
| 项 | before | after |
|---|---|---|
| 用户抽屉 | 触摸点不开（`checked=false`，面板 x=396 在屏外） | 触摸可开（`checked=true`，x=90，遮罩正常） |
| 点用户的菜单 | `z=500`、被抽屉盖住、`onTop=false` | `z=700`、`onTop=true`、抽屉自动收起 |
| @提及（菜单） | 输入框仍为空、焦点在 BODY | 输入框 `@昵称 `、焦点 `msg-input`、光标 21（末尾） |
| @提及（行内按钮） | 不存在该按钮 | 42×36 按钮，一击插入 + 聚焦 + 收起抽屉 |
| 私聊窗口 | 打不开（菜单点不到） | 行内 🔒 一击打开，rect 390×844（`fullscreen=true`） |
| 未读角标 | 收到私聊后 `null` | `1`，打开窗口后清零 |
| 设置页行数 | 22 行（含 6 项无用 + 4 个不存在的工具栏勾选） | 13 行（工具栏勾选只剩真有的两个） |

## [2.8.2] - 2026-09-24

> 主题：**静默运行**（桌面不再挂 4 个窗口）+ **托盘状态提示**（换一种方式告诉你服务正不正常）。

### 静默运行（推荐日常使用）
- 新增 **`静默运行Uchat.vbs`**：双击即用，全程**隐藏窗口**拉起守护进程，桌面上一个窗口都不会多
- 新增 **`静默运行Uchat.ps1`**（守护进程）：单实例互斥 + 服务看门狗（3 秒一轮，java 不在就拉起）
  + 阻止系统空闲休眠（等价于原 `保持唤醒.ps1`）+ TURN + 两个 AI；并顺带保证托盘监视活着
- 新增 **`静默监视Uchat.ps1`**（托盘图标）：每 5 秒探测一次 `/api/health`
  - 🟢 正常 / 🟡 探测抖动一次 / 🔴 连续两次无响应；**掉线与恢复都会弹气泡提醒**
  - 悬停显示「Uchat 运行正常 · 已运行 xh xxm（xx ms）」；右键菜单：打开 Uchat / 打开日志目录 /
    立即检查 / 重启服务 / 停止 Uchat（全部）/ 退出监视
  - 探测刻意不用 `Invoke-WebRequest`（本机部分会话里 PowerShell 的 HTTP 栈不可靠），
    改用 `TcpClient + SslStream` 手写 HTTP/1.1 请求（自签证书照收）
- 新增 **`静默停止Uchat.ps1`**；`停止Uchat.bat` 会先调它 —— **必须先停守护进程**，
  否则它会在 3 秒内把刚停掉的服务重新拉起来
- 状态留痕：`logs\_service_status.json`（守护进程每轮写）、`logs\_status_monitor.log`（每次探测 + 状态变化）、
  `logs\_silent_supervisor.log`（拉起/重启记录）、`logs\_silent_stop.log`（停止记录）
- `稳定运行Uchat.bat`（窗口版）保留，行为不变，供排障时直接看日志

## [2.8.1] - 2026-09-24

> 主题：**两处上线后反馈的修正**（都不涉及数据正确性，但直接影响观感/可用性）。

### 修复：状态条上「更早的消息已无法恢复」的误报
- **根因**：断线补齐的截断判定原来是 `!hasTimed || since < oldest`，把**空缓冲区**也判成截断 ⇒
  清空聊天记录之后，任何带着旧 `since` 重连的客户端都会拿到 `truncated=true`，前端状态条随即冒出这句提示
  （而其实一条都没丢）。
- 判定抽成 `ChatWebSocketHandler.isTruncated(hasTimed, since, oldest, bufferEmpty)`：**缓冲区为空 ⇒ 不截断**；
  非空但无时间戳 ⇒ 保守标记；否则维持「since 早于最早一条才算截断」。新增纯单元测试 `SyncTruncationTest` 覆盖四种分支。
- 客户端：**状态条不再显示这句话**（用户要求）。信息不丢 —— 仍写 console 日志，并作为状态条的 `title` 悬停可见。

### 修复：看门狗窗口里 java 日志全成乱码
- **根因**：`稳定运行Uchat.bat` / `启动Uchat.bat` 用 `start "" /b` 启动 bot / TURN 的启动器，它们与看门狗
  **共用同一个控制台**；子进程里的 bun / powershell 会把该控制台代码页改成 UTF-8(65001)，而 java 按 GBK 写日志
  ⇒ 窗口里 java 的行全成乱码（实测：该窗口 `OutputCP=65001`，屏上是 `�Լ��˺�_...` 这类）。
- 改法：bot / TURN 启动器改为 `start "Uchat Bot XXX" /min cmd /c ...`（各用**自己的**控制台，页面代码页互不影响），
  并在启动 java 之前补一句 `chcp 936 >nul` 兜底。
- 附带效果：`停止Uchat.bat`（按窗口标题 `Uchat*` 杀）现在会把 bot / TURN 窗口一并停掉，下次启动服务时自动拉起来。

## [2.8.0] - 2026-09-24

> 主题：**P0-④ 离线私聊** —— 私聊不再丢。改造前 `private` 消息只发给收发双方、**不落库**，
> 对方离线时直接回 `private_fail`（消息彻底丢失），断线重连也补不回来。本版把私聊做成与公聊同级的可靠消息。

### 服务端
- 新增 `PrivateStore`（`model/PrivateStore.java`）：每条私聊都落盘 `data/private.dat`
  （临时文件 + 原子替换；1 秒节流 + 10 秒兜底线程），容量为全局 2000 条 / 单收件人 200 条
- `handlePrivate`：**对方离线不再失败** —— 入库 + 回 `chat_ack`；在线则顺手直发并标记已投递。
  仍 fail 的只有「收件人不是注册用户」「给自己发」，且 `private_fail` 现在带 `msgId`（客户端据此精确撤回那一条）
- 上线补投：登录时先下发 `private_pending`（未读总数 + **按发件人分组**的条数 + 补不齐的条数），
  再逐条补投带 `offlineMsg:true` 的原文，最后标记已投递（**不会重复补投**）
- `sync_since` 除公共 history 外，还能按时间戳补齐**本人涉及的**私聊（自己发的 / 发给自己的），
  可见性由存储层保证 —— 第三方一条都拿不到

### 客户端
- 私聊窗口：**对方离线也能打开、也能发**（旧版直接弹「当前离线，无法私聊」并拦下）；窗口标题与提示行标出离线状态
- 本地私聊历史按 `msgId` 去重（同一条可能被「在线直发 / 登录补投 / sync 补齐 / 自己的回显」多次送达）
- 未读角标：显示在用户列表的联系人名字后面，打开窗口即清零；服务端下发的未读数只作"下限兜底"，与本地计数取 `max`（避免登录时被算两遍）
- 服务端补投的离线消息打 `[离线]` 标记，且**不自动弹窗**（避免一登录被一串窗口刷屏）
- 私聊发送接入 Outbox：断网/服务重启期间的消息进 localStorage 队列，重连后按同一 `msgId` 重发（服务端幂等去重）
- 新手教程补一条「私聊支持离线」（教程版本 3 → 4，老用户会重看一次）

## [2.7.2] - 2026-09-24

### 界面与体验
- 用户列表：按「管理员 → 机器人 → 普通用户」分组，组内按首字母（中文按拼音，ICU 排序）
- 用户列表：单击某个 id 弹出操作菜单（私聊 / 静音 / @提及），与聊天区右键完全一致；双击仍为 @提及
- 新手教程内容重写（覆盖 AI 用户、角色标识、单击菜单、共享声音、跨网络等新能力），
  教程版本 2 → 3（老用户会重新看到一次）；新增 APP_VERSION 常量作为版本号单一来源

### 聊天
- 登录 / 重连等瞬时系统提示不再写入聊天历史（此前被持久化，导致重新登录被刷屏）
## [2.7.1] - 2026-09-24

### 修复
- 登录/重连等瞬时系统提示不再写入聊天历史：此前它们被持久化到 data/room.dat，
  用户重新登录时会被历史里积压的「XXX 已重连 / 进入聊天室」刷屏（清聊天记录也清不掉）。
  现以 addHistoryIfWorth() 过滤 welcome 与瞬时系统消息；并清理了既有 room.dat。

## [2.7.0] - 2026-09-24

### 用户列表角色样式
- 新增 chat.roles.admin / chat.roles.bot 配置，服务端在 userlist 消息里下发 roles 映射
- 前端按角色区分样式：管理员（👑 金色左边框+加粗）、机器人（🤖 青色虚线左边框+斜体）、普通用户不变；
  在线与离线行均生效，含暗色主题适配
- 踩坑：Spring 按 ISO-8859-1 读 .properties ⇒ 中文昵称必须写 \uXXXX 转义

### AI 用户（群内机器人）
- 修复"@ 没反应"：补心跳（客户端协议每 5 秒一次，连续 3 次无响应主动重连）、
  登录后 8 秒暖机期 + 按 msgId 时间戳过滤历史回放（避免重连时把旧的"闭嘴"指令重放导致静默）、
  模型调用加硬超时与队列看门狗（防单条卡死拖垮整队）
- 修复"重复作答"：bot 自己的回复以 assistant 角色记入上下文，只针对最后一条作答
- 新增第二个 AI 用户「资料鱼」：仅被 @ 触发、详细作答并给出可查证的参考链接；
  chatbot.mjs 支持 --base（多实例隔离）、mentionRequiresAt、apiKeyLine、格式安全切片
- 启动器查重改为按进程命令行判断（防重复实例互相顶号）；bot.lock 健壮化

## [2.6.0] - 2026-09-23

### 通话与音频（本轮重点）
- 修复语音通话"进房十几秒后掉线""两端状态不一致"：服务端在被移出时通知本人、
  正常离开时通知房间成员；客户端收到自己的 room_leave 只做本地收尾不再回发（断开回声环）
- 修复"两台设备之间永远连不上"：服务端清理残留的通话房间条目（曾导致信令被判跨房间而丢弃）
- 新增本地 TURN 中继（UDP 3478，随服务自动启动）与公共 TURN 兜底，实现跨网络通话
- 屏幕共享支持系统音频；修复"与距离无关、越叠越大的回声"（共享端本地预览必须静音）
- 修复 iPhone"听不到对方语音"（纯音频流改用 <audio> 播放）与"发出去的是静音"
  （AudioContext 挂起时改用原始麦克风轨）
- 麦克风静音改为"真闭麦"（释放采集），解除时自动重新采集并换回发送轨
- 音频会话按需切换（通话中开麦 play-and-record / 闭麦 playback / 其余 auto）
- 通话中若本地电平持续为 0，界面直接给出麦克风排查指引

### 移动端
- 工具栏精简为：表情、骰子、语音、视频、设置、日夜切换、清屏（+在线列表）
- 视频/语音通话面板与屏幕共享面板适配手机（贴边、可拖动、不再跑出屏外）
- 共享观看栏新增：共享声音开关、麦克风开关；手机端隐藏 LaTeX 公式相关按键
- 输入栏提示按屏宽自适应（手机不再出现 Enter/Ctrl 字样）
- 切屏回来不再黑屏；修正最小化/最大化图标

### 其他
- 每个通话房间上限 4 → 6 人
- 静态资源加版本号，避免"客户端跑旧代码"
- 清理全部临时通话诊断输出

### 安全与运维
- 密钥/口令一律仅本人可读（keystore.pwd / creds.env / turn.pwd），且不入版本库
- 启动脚本改为单实例守卫；本地 TURN 随服务启动

# Uchat 更新日志

## v2.5.0 (2026-09-23)

> 主题：**离线可用 + 消息可靠性**。规划与任务书见 WORKPLAN.md / ROADMAP.md（**这两份已于 2026-09-24 清理**，需要时可在 Git 历史里查看：`git show v2.5.0:WORKPLAN.md`）。
> 本轮为「落地改造」第 1–3 项：① Git 仓库化 ② CDN 依赖本地化 ⑤ 消息可靠性（服务端 + 客户端）。
> 手机端（原任务 4/5/6）按用户要求暂缓，任务书保留在 `prompts/`。

### 🌐 离线可用：前端第三方库全部本地化（T1）
- `index.html` 里 6 个 `cdn.jsdelivr.net` 依赖全部下到 `static/vendor/`（27 文件 / 729,996 B）：
  marked 15.0.12、highlight.js 11.9.0（js + github-dark 主题）、KaTeX 0.16.11（js + css + auto-render + **20 个 woff2 字体**）。
- **局域网 / 内网 / 断网环境下 Markdown、代码高亮、数学公式不再失效**（此前会整块失效）。
- 独立复核（自建 before/after 两棵树 + `--host-resolver-rules` 屏蔽 CDN）：
  改动前 `typeof marked/hljs/katex` 全 `undefined`、`renderMarkdown()` 只回显 44 字符纯文本；
  改动后全部 `object`、产出 `<strong>` + `class="katex"` + `language-js`（1252 字符），对 CDN 的请求数 **6 → 0**。

### 📨 消息可靠性 · 服务端（T3）
- **幂等去重**：按 `(昵称, msgId)` 去重，重复提交**不再重复入库 / 重复广播**（10 分钟窗口 + 60 秒清理 + 2 万条上限，最坏约 3.6 MB）。
- **`chat_ack` 回执**：告诉发送方「已处理」，`duplicate:true` 表示此前已处理过 ⇒ 客户端据此出队，这是"不重"的关键。
- **`sync_since` → `sync_result` 断线补齐**：重连后可拉取错过的消息（含四种边界：全量 / 起点过早 `truncated:true` / 晚于最新返回空 / 非法值不抛异常）。
- 新增集成测试 `ChatReliabilityTest`（12 个用例，`@SpringBootTest` + JDK 自带 `java.net.http.WebSocket`，零新依赖）。
  **`mvn -B clean test` → Tests run: 36, Failures: 0**（原有 24 + 新增 12）。
- 协议字段与语义见 [DOCS.md §六](DOCS.md)。

### 📤 消息可靠性 · 客户端（T4）
- 新增 `js/outbox.js`（711 行）：**发送队列 + localStorage 持久化 + 指数退避重发 + 只出队一次的 ack 处理**。
- 断网 / 切网 / 服务重启期间发出的消息**排队并在恢复后自动送达，且不重复**；
  **刷新页面后队列仍在**（同一条重发复用同一 `msgId`，服务端据此幂等）。
- 重连后自动 `sync_since` 补齐；收到重复 `msgId` 不再二次渲染；消息旁显示「待发送 / 失败可重试」。
- 端到端验收（真实浏览器 + CDP，47 条断言）：改动前 **PASS=26 / FAIL=21** → 改动后 **PASS=47 / FAIL=0**；
  `room.dat` 交叉核对：10 条 chat、唯一 msgId 10、**无任何 msgId 出现两次**。

### 🐛 修复
- **`chat.js` 代码块语法着色失效**：仍用 marked v5 已移除的 `highlight` 选项（被静默忽略），
  代码块只有 `class="language-js"` 而没有 `<span class="hljs-*">`。改为入 DOM 后对 `.md-rendered pre code`
  调 `hljs.highlightElement()` 并打 `data-hljs` 标记保证幂等（实测 9 个 span、keyword 色 `rgb(255,123,114)`、二次调用不嵌套）。
  此缺陷**早于本轮存在**（本地 marked 与线上那份逐字节相同）。
- **`app.js`：`onLoginSuccess()` 每次登录都抛异常**。`applyToolbarVisibility()` 被误声明在
  `initChangePwdUI()` 函数体内部 ⇒ 调用它抛 `ReferenceError`，被 `chat.js` 的 `ws.onmessage` 外层 catch 吞掉。
  后果：该函数之后的语句（媒体权限预请求、focus 监听）**永不执行**，调用方的 **`startHeartbeat()` 也被跳过**。
  已把该函数整块搬到顶层（函数体一字未改），bun 解析通过、端到端 47/47 复验。
- **`mvn test` 不带 `clean` 连跑第二次必挂 3 个用例**（`users.dat` 残留）：`UserManagerTest` 现在自己清理残留，
  实测**不带 clean 连跑 3 次全绿**（36/36 ×3）。

### 🔧 工程
- **引入 Git 仓库**（首个受控版本 tag `v2.4.0`，本轮打 `v2.5.0`）：跟踪 58+ 文件、仓库仅 269 KiB；
  `.gitignore` 排除 `java/`、`uchat.jar`、`target/`、`backup/`、`data/`、`logs/`、`keystore.p12`；
  `.gitattributes` 强制 `*.bat/*.cmd/*.ps1` 用 **CRLF**（LF 的 bat 会让 cmd 解析错乱）。
- 新增 `dev/` 端到端验收工具（`t4_harness.js`：CDP 驱动真实浏览器 + 自建 8899 实例，47 条断言）。
- ~~`WORKPLAN.md`~~ / ~~`ROADMAP.md`~~（落地计划与产品化评估，**已于 2026-09-24 清理**，见 Git 历史）、`prompts/`（任务书与证据）。
- ⚠️ 已知限制（登记在 `prompts/_待协调.md`，待拍板）：
  `Room.MAX_HISTORY=200` 使超出部分补不回来；**私聊不落 history**（对方离线即丢，属"离线消息"未做）；
  密码字符集在「模型层 / WebSocket 注册分支 / CHANGELOG」三处不一致。

### 🔐 安全加固：keystore 口令脱离明文（2026-09-23）
- `server.ssl.key-store-password` **不再带明文默认值**，只从环境变量 `UCHAT_KEYSTORE_PWD` 读取；
  缺失或错误时应用**直接启动失败**（fail-closed，实测报 `keystore password was incorrect`）——
  去掉了"默认口令兜底"这种把私钥送给任何拿到源码的人的设计。
- `keystore.p12` 已用新生成的 24 位随机口令重建（别名仍 `uchat`）；旧 p12 备份在 `backup\keystore_*.p12.bak`。
- 口令存放：用户环境变量 `UCHAT_KEYSTORE_PWD`（主）+ 项目内 `keystore.pwd`（本机兜底，**已进 .gitignore**，
  并用 `icacls` 收紧到仅当前用户可读）；两个启动 bat 在环境变量缺失时会自动读取 `keystore.pwd`。
- `keystore.p12` 与 `keystore.pwd` 均不在 git 跟踪范围内（已核验）。

## v2.4.0 (2026-09-22)

> 主题：**音视频通话可靠性 / 音质 / 画质加固** + **局域网稳定性与重连机制重写**。
> 详细改动清单与验收步骤原见 FIXES.md / 审查报告 REVIEW_CALL.md（**两份已于 2026-09-24 清理**，见 Git 历史：`git show v2.4.0:FIXES.md`）；
> 局域网部署的排查与调优见 [DEPLOY.md §4.3](DEPLOY.md)。

### 🧪 构建与测试（2026-09-23）
- **修掉控制台中文乱码**：`启动Uchat.bat` / `停止Uchat.bat` / `稳定运行Uchat.bat` 原本是
  UTF-8 文件 + `chcp 65001`，但 **JDK 17 的 `System.out` 按 `file.encoding`（本机 GBK）输出字节**，
  控制台却按 UTF-8 解释 ⇒ 满屏乱码（`注册码` 的 GBK 字节 `D7 A2` 被读成 `ע`）。
  三个 bat 已统一为 **GBK + CRLF + `chcp 936`**，与 Java 的输出编码一致。
  A/B 实测：旧写法同一份输出里「cmd 自己的回显是 UTF-8、Java 的行是 GBK」，怎么读都是乱的；
  新写法整份统一 GBK，控制台中文正常。
  注：应用写的 `logs\server_<时间戳>.txt` **一直是干净的 UTF-8**，不受此问题影响 ——
  乱码只出现在**控制台窗口**，从控制台复制的文本才会带上乱码。
- **单测不再"只有全新检出才绿"**：`UserManager`/`Room` 把数据写到 `user.dir\data`，而 `mvn clean` 只删
  `target\` —— 第一次 `mvn test` 通过、第二次因 `UserManagerTest` 的固定用户名（alice/bob/eve）已存在
  而失败 3 个用例。已给 surefire 配 `workingDirectory=${project.build.directory}`，
  测试数据落到 `target\data\`，可反复执行（已连跑两次验证 24/24 全绿）。
- **新增 `构建Uchat.bat`**：一键构建 + 部署（用项目自带 JDK 17、显式重置 PATHEXT、
  成功后把旧 `uchat.jar` 备份进 `backup\` 再替换），实跑 4 次验证。
- 首次用 Maven 完整验证：**17 个主源码 + 4 个测试类编译通过，24/24 单测通过**，
  产物经 `spring-boot:repackage` 打完并实跑（`Started ChatRoomApplication v2.4.0`、
  `/api/health` 200、`/api/turn` 200）。
- **`使用说明.txt`（备份包总说明）补上注册码**：原文只说"请自行注册账号"，没给注册码，
  拿这份备份去别的机器的人会卡住。现已写明「注册码：`237590`（固定值）」。

### 🔁 局域网稳定性 / 重连机制（本次重点）- **重连退避重写**：旧版是**固定 30 秒一次、且第一次也要等满 30 秒**，重试 10 次（5 分钟）后
  `goToLogin()` 把用户踢回登录页并清空密码框 —— 主机重启一次，房间内所有人被迫重新输密码。
  现在：首次约 **0.5 秒**重试，指数退避 `1/2/4/8/15` 秒封顶 15 秒，±25% 抖动，**永不放弃**。
  实测主机离线 2 秒的恢复时间 **30.0 s → 3.5 s**；离线 600 秒（≈主机重启）旧版放弃、新版 **608 s 自动恢复**。
- **心跳 10 秒 → 5 秒**：把"TCP 半开（没有 RST）"的发现时间从 30 秒压到 15 秒。
- **事件驱动立即重试**：`online` / `visibilitychange` / `focus` / `pageshow` 触发即刻重连
  （后台标签页里浏览器把 `setTimeout` 节流到 ≥1 分钟，这些事件是必要补充）。
- **连接代次号**：旧 socket 迟到的 `onclose` 不再影响刚恢复的会话（否则重连成功又被误判离线）。
- **认证看门狗**：连上但 10 秒收不到 `auth_resp` 强制重试，不再卡死在"正在重连"。
- **重连期间认证被拒不退出登录**：保留凭据继续退避重试（旧版直接进登录页）。
- **服务端：写失败即关闭连接**（`UserSession.send`）：旧版只打印一行日志就返回，
  服务端会一直以为该用户在线（直到容器 idle 超时 30 分钟），期间用户列表/广播/重连判定全部基于错误状态。
- **服务端：快速重连不再刷屏**（`supersededSessions`）：主机抖动后所有客户端几乎同时回来，
  被顶替的旧 socket 的关闭事件会晚于新连接到达，旧版会给每个人刷一轮
  "X 断开了连接 / X 已重连"；现在静默处理。
- **新增 `稳定运行Uchat.bat`**：`:loop` 守护外壳，JVM 崩溃/被杀后 3 秒自动重启，并在每轮启动前清理端口残留。
- **新增 `保持唤醒.ps1`**：用 `SetThreadExecutionState` 阻止主机空闲休眠（**免管理员**、不改电源计划），
  默认只阻止系统休眠、显示器仍可自动关闭；`-KeepDisplayOn` 可连屏幕常亮。

### 🔴 安全修复
- **视频信令补齐鉴权**：`video_offer/answer/ice` 是全文件唯一没有 `isSessionOwner` 校验的 handler（其余 22 处都有），
  且 `receiver` 不校验是否同房间、完全不计限流。现在要求「会话归属 + 接收者在线 + 载荷上限 + 收发双方同房间 + 单独限流」五重校验；
  任何一项不满足直接丢弃并记日志。
- **屏幕共享信令加固**：`screen_offer/answer/ice` 同样加了身份、接收者、载荷与限流校验，
  并去掉「receiver 为空就全局广播」的放大路径。
- **keystore 口令外移**：`server.ssl.key-store-password` 改为 `${UCHAT_KEYSTORE_PWD}`，
  可用环境变量覆盖，不再强制明文写在配置里。

### ✨ 新功能
- **TURN/STUN 下发接口** `GET /api/turn`（新增 `TurnController`）：
  按 coturn 的 `use-auth-secret`（REST）机制下发 1 小时有效的 TURN 临时凭据 + 可配置的多条 STUN；
  未配置 TURN 时只回 STUN，行为与旧版一致。
- **通话掉线宽限期**：掉线不再立刻移出通话房间，席位保留 45 秒（可配 `chat.call.grace-ms`），
  对端收到 `room_peer_unstable` 显示「⏳重连中」而不是拆掉连接；重连后发 `room_peer_resume`。
- **媒体状态可视化**：新增 `media_state` 消息，麦克风/摄像头开关实时同步给同房间成员，
  对端画面上显示 🔇 / 📸 角标（此前对端只看到黑屏，分不清「关了」还是「卡了」）。
- **ICE restart 自愈**：`disconnected` 超时 4 秒或 `failed` 时自动 `restartIce()` 重新协商；
  监听 `window.online` 在网络回切时主动重建连接（旧版只能手动退出重进）。
- **逐对等体码率自适应**：按人数给每路上行设码率/帧率上限，并按实时丢包率/RTT 自动降档
  （降码率 → 仅语音）与恢复；质量角标同时显示丢包率、RTT、`qualityLimitationReason`（↓带宽/↓CPU）。

### 🔧 改进
- **音频约束打开浏览器原生处理**：`echoCancellation` / `noiseSuppression` / `autoGainControl` 由 **false 改为 true**，
  并设 `channelCount:1` / `sampleRate:48000`。旧版三项全关 → **完全没有回声消除**（对方外放必啸叫）、无 AGC。
- **「AI 降噪」更名并降级为可选增强**：原实现只是高通+压缩器+软扩展器，并非 AI；
  现在浏览器原生处理是主体，自研链退居「增强降噪」档位，默认关闭。
- **音频处理链收敛为一条**：旧版「麦克风增益链」与「降噪链」相互独立且叠加顺序不定，
  且 `enableNR` 只 `replaceTrack` 已有 sender → **之后新建的 peer 拿到未降噪轨道，同房间音质不一致**。
  现在统一为 `源 → [增益] → [可选增强] → 输出`，`myStream` 始终暴露链输出。
- **ICE candidate 排队**：远端描述就绪前收到的候选改为排队、`setRemoteDescription` 后统一 flush，
  不再抛 `InvalidStateError` 后静默丢弃。视频与屏幕共享两条链路都修了。
- **Opus 抗丢包**：SDP 里为 Opus 打开 `useinbandfec=1`（带内 FEC）、`usedtx=1`（静音停发）、
  `stereo=0`、`maxaveragebitrate`（1对1 64k / 多人 32k）。
- **关摄像头真正停发**：改用 `sender.setParameters({encodings:[{active:false}]})`，
  旧版 `track.enabled=false` 仍在持续发送黑帧。
- **房内打开摄像头会重协商**：进房时未开摄像头、之后点 📷 打开的场景，
  旧版只 `addTrack` 不重协商 → 对端永远收不到画面；现在显式补一次 offer。
- **进房不再盲重发**：服务端在 `room_enter` 后立即直发 `room_state`；
  前端去掉无条件的 3 秒重发（改为「5 秒内完全没收到状态才补发一次」）。
- **房间状态广播去抖**：150ms 合并重复广播。
  旧版用 `broadcastingRoomState` 布尔量直接**丢弃**并发广播 —— 两人同时进出房间时后一次被吞掉，
  那批用户永远收不到 `room_state`（这正是前端要盲重发兜底的真因）。
- **房间状态判定修正**：`room_state` 不再用 `count <= 0` 判断该不该挂断，
  改为「自己是否仍在 `users` 列表中」。
- **屏幕共享**：视频轨设 `contentHint='text'`（共享文字/代码明显更清晰）、
  按帧率设码率上限并 `degradationPreference='maintain-resolution'`（弱网先掉帧不掉清晰度），
  帧率/清晰度切换时同步刷新码率。
- **AudioContext 泄漏修复**：旧版每个远端 peer 各建一个 `AudioContext` 且从不 `close`，
  反复进出多人房会累积到浏览器上限后说话检测静默失效；现在全页面共用一个。
- **统计口径修正**：丢包率改为**增量式**（旧版用累计值，刚断流时仍显示 100%），
  RTT 取 `nominated` 的 candidate-pair（旧版可能取到未使用的候选对）。
- **提示与文案**：进房不再用阻塞式 `confirm()` 询问是否开摄像头，改为面板上的勾选框并记忆选择；
  自动播放被拦截时提示可点击画面播放。
- **日志降噪**：`video_ice`/`screen_ice`/`media_state` 不再逐条打印 `[MSG]`（ICE 频率很高）。

### 🐛 Bug 修复
- **修掉 `playsinline` 拼写错误**：正确属性名是 `playsInline`（旧代码 `video.playsinline = true` 只是挂了个无效 expando），
  iOS Safari 下远端画面会全屏弹出。现在同时设属性与 IDL。
- **修掉无效的「Prefer H.264」代码**：旧版执行 `sender.setParameters(sender.getParameters())` 等于什么都没做，
  改用真正的 `RTCRtpTransceiver.setCodecPreferences()`。
- **修掉麦克风增益初值快照**：`savedMic` 是模块加载时的快照，滑块改过之后进房会用旧值；改为进房时实时读取。
- **修掉通话宽限期与聊天不一致**：聊天的断线宽限期是 6 分钟，通话却是「掉线立即移除」，
  WiFi 抖 3 秒整场通话就废了。

### 🗑️ 移除
- **删除 WebSocket PCM 语音中继**（`handleVoiceJoin`/`handleVoiceLeave`/`handleVoiceData`/`broadcastVoiceState`，
  约 80 行）：前端从无任何地方发送 `voice_join`/`voice_data`/`voice_leave`，属死代码。
  且它把 raw PCM 经 WebSocket 广播给所有参与者，若被误启用会与 WebRTC 音频**双路重叠**产生回声。
- 删除 `voice_state` 前端处理分支、`broadcastingRoomState` 布尔重入锁（功能由去抖机制取代）。

### ⚙️ 配置
- **通话房间人数上限 8 → 4**（`chat.call.max-per-room`）：纯 P2P Mesh 下 8 人需要每端 7 路上行
  （≈4 Mbps 纯视频流），家庭宽带上不可行。旧版的「缓解」是 ≥6 人**静默关掉所有人的视频**，
  等于把功能取消且不给任何提示；现在容量与实际能力对齐。
- `启动Uchat.bat` 堆上限 `-Xmx256m` → `-Xmx768m`（100MB 上传 + 8 路信令的堆压力）。

---

## v2.3.1 (2026-06-26)

### ✨ 新功能
- **番茄钟完成特效**：全部轮次完成后触发，75% 金色 / 25% 蓝紫粉渐变随机，格子流光 + 烟花扩散 + 计时器变色
- **番茄钟格子视图**：每分钟/格，工作红/休息绿，切换按钮 ▦/━，自动适配格子数量上限（≤60）
- **番茄钟重置保存**：重置时已专注 ≥1 分钟自动记录
- **番茄钟今日柱状图**：最近 7 天每日时长，内嵌在番茄钟面板中

### 🔧 改进
- **番茄钟全自动**：点一次开始，工作→休息→工作自动循环，无需手动干预（暂停仍可用）
- **后台计时修复**：改用 `Date.now()` 替代 `setInterval` 计数，切后台时间准确
- **弹窗拖拽修复**：`mousedown`/`click` 双重检查，拖拽超出窗口不再误关闭
- **启动简化**：回退托盘方案，`/MIN` 最小化到任务栏，稳定可靠

### 🐛 Bug 修复
- **番茄钟多余休息**：最后一轮工作后不再进入休息，N 轮 = N 次工作 + (N-1) 次休息
- **格子状态同步**：elapsed 计算适配新的 `_tmPomoCount` 计数逻辑
- **完成格子残留**：`_tmSec` 完成时置为 `_tmWorkSec`，重开界面不误填

### 🗑️ 移除
- 系统托盘 PowerShell 方案（`server-tray.ps1`/`watchdog-tray.ps1`）

---

## v2.3.0 (2026-06-26)

### ✨ 新功能
- **用户自定义状态**：📝 按钮设置当前状态（学习中/工作中/忙碌/离开/听歌中/游戏中 + 自定义），状态标签显示在用户列表，实时同步给所有在线用户
- **番茄钟统计服务端持久化**：累计轮数和总时长保存到 `data/users/{nickname}/pomodoro.json`，服务端重启不丢失；`PomodoroController` 提供 GET/POST 接口
- **健康检查端点**：`GET /api/health`，返回服务状态、运行时间、Java 版本、内存使用
- **看门狗守护进程**：每 10 秒检测服务器健康状态，连续 3 次失败自动重启服务器（杀旧进程树 → 拉起新进程），首次失败输出 `curl -v` 详细诊断
- **系统托盘启动器**：PowerShell 托盘图标（🖥️ 服务器 / 🛡️ 看门狗），左键切换窗口显隐，右键菜单 Stop & Exit（自动生成诊断转储到 `logs/diag/`）
- **诊断转储**：关闭时自动输出健康检查结果、端口状态、Java 进程信息、最近日志到 `logs/diag/server-stop-*.txt`
- **测试端启动器**：`test-client.bat` 一键打开稳定性测试面板

### 🔧 改进
- **自动重连优化**：间隔从 15s 延长到 30s（10 次共 5 分钟），服务端宽限期从 3 分钟延长到 6 分钟
- **重连消息去重**：`history_start` 时清空聊天区，避免重连后历史消息重复显示
- **僵死连接处理**：WiFi 断开后 TCP 未超时导致 `hasUser()` 误判在线，现改为对比 WebSocket session ID 主动踢旧连
- **心跳日志静默**：服务端不再为每条心跳输出 `[MSG]` 日志
- **启动脚本优化**：`mvn package -Dmaven.test.skip=true` 跳过测试编译；移除 VBS 启动器；重命名为 `server-run.bat`/`watchdog-run.bat` 内部脚本
- **诊断文件 UTF-8 BOM 编码**：解决中文 Windows 记事本打开乱码

### 🗑️ 移除
- VBS 启动器（`start-server.vbs`、`watchdog.vbs`）

---

## v2.2.0 (2026-06-25)

### 💥 重大变更
- **移除插件系统**：删除 `Plugin`/`PluginManager`/`PluginContext`/`PluginController` 全链路，消除 URLClassLoader 加载外部 JAR 的安全风险
- **移除倒计时/正计时模式**：番茄钟简化为纯番茄模式，专注核心功能

### 🔒 安全加固
- **CORS 可配置**：新增 `CorsConfig` 全局配置，`chat.allowed-origins` 属性替代硬编码 `*`，部署时可锁定具体域名
- **文件下载鉴权**：下载/预览接口新增 `AuthTokenStore` token 校验，登录后下发 64 位随机 token，未认证请求返回 403
- **注册码验证**：注册需填写 6 位固定注册码 `237590`，服务端 WebSocket + HTTP 双路径校验
- **修改密码鉴权修复**：登录页修改密码不再依赖 `isSessionOwner`（临时 WebSocket 无 session 关联），改为直接密码验证

### ✨ 新功能
- **番茄钟增强**：每日目标环形进度条（SVG）、工作/休息阶段长进度条（色块分段 + 圆点指示）、累计统计（localStorage 按昵称隔离，自动跨日清零）
- **稳定性测试面板**：`/test.html` 多用户模拟器，支持快速重连/双人断线/通话切换/压力重连/浸泡测试 5 种场景，可选 2~10 人
- **页面装饰更新**：登录页版本号显示（v2.2.0）、副标题改为 "Develop · Test by HGDTeam"、底部著作权标注

### 🐛 Bug 修复
- **WebSocket 15-20 分钟断线**：`maxSessionIdleTimeout` 10min→30min；客户端心跳新增连续丢失检测（3次30秒无响应主动重连）
- **通话重连后房间分裂**：新增 `rejoinCallRoom()` 函数，重连时设置 `justJoinedCall=true` 触发 WebRTC 重协商
- **修改密码卡在"连接中..."**：服务端 `handleChangePwd` 对未认证 session 改为密码验证；`chat.js` 新增 `auth_change_pwd_ok/fail` 消息处理；应用内修改密码移除假成功 toast
- **房间容量被污染**：`room.dat` 中 `maxCapacity=4` 覆盖代码常量 16，改为始终创建新 Room 仅保留历史消息
- **新手引导失效**：localStorage 标记改为版本号机制；重放按钮清除标记确保每次弹出
- **篝火音效像爆米花**：重写三层算法（低频轰隆 + 稀疏噼啪 + 偶尔爆裂），大幅降低频率和音量

### 🔧 改进
- **屏幕分享设置**：清晰度改为 480p/720p/1080p（移除 1440p），帧率改为 15/30/60/90/120fps（高帧率标注带宽警告）
- **前端协议自适应**：`getApiBaseUrl()` 根据 `location.protocol` 自动选择 `http:`/`https:`，不再硬编码
- **环境音篝火重写**：三层合成（褐噪声底噪 + 低频噼啪 + 偶尔爆裂），频率降低 99%+，音量降低 85%+
- **注册码输入框**：登录页新增注册码输入框，仅注册标签页显示
- **数据清理**：清空所有测试聊天记录和用户信息

---

## v2.1.1 (2026-06-20)

### 🔒 安全加固
- **密码哈希升级为 PBKDF2**：100,000 次迭代 + HMAC-SHA256，替换原有的单次 SHA-256。旧格式密码自动迁移升级
- **密码字符集开放**：移除「仅字母数字」限制，现允许任意字符（6-18 位）
- **访客模式移除**：删除房间码/访客登录功能，仅允许注册账号进入。清除前端访客 UI 和后端 guest_auth 全链路
- **PrefsController 路径穿越防护**：拒绝含 `..`、`/`、`\`、空字节的昵称，限制 64KB 数据上限
- **撤回时限绕过修复**：msgId 格式异常时直接拒绝撤回，不再静默放行

### 🐛 Bug 修复
- **静默异常不再吞没**：`UserSession.send()`、`Room.saveToFile()`、`UserManager.save/load()`、`PrefsController` 所有 I/O 异常改为输出 `System.err` 日志
- **密码校验前端同步**：移除 JavaScript 端的字母数字校验，与后端策略一致

---

### ✨ 新功能
- **插件框架**：Java 端 `Plugin` 接口 + `PluginManager`（JAR 加载/生命周期/消息路由）+ REST API；前端 `ChatPlugin` 系统（`plugin.js`）+ 动态脚本加载 + 服务端同步
- **通话房间拆分**：视频和语音各自独立两间（共 4 个房间），每间最多 8 人，加入前可看到各房间人数
- **纯语音通话**：🎙️ 按钮进入语音模式，紧凑 UI（仅控制栏）+ 参与者列表 + 网络质量角标 + 实时说话检测高亮
- **粘贴图片**：输入框 Ctrl+V 粘贴图片自动上传发送
- **登录设备授权**：登录成功后自动请求摄像头和麦克风权限

### 🐛 Bug 修复
- **修复屏幕共享观看者黑屏**：`screen_offer/answer/ice` 改为定向路由（`room.sendTo(receiver)`），前端增加 `receiver` 校验
- **修复摄像头画面消失**：移除屏幕共享时隐藏视频 tile 的逻辑；WebRTC `disconnected` 状态不再移除 tile（仅 `failed` 移除）
- **修复视频信令跨房间干扰**：`video_offer/answer/ice` 改为房间内广播，防止 A 房间信令干扰 B 房间
- **修复 room_leave 缺少 subtype**：`leaveCallRoom` 和 `hangupVideoCall` 均携带 `subtype` 字段，服务端正确清理对应房间
- **修复 leaveCallRoom 中 audioOnlyMode 顺序 bug**：取值在重置之前完成
- **修复共享者预览窗口关闭后无法重新调出**：再次点击按钮重新绑定 `screenStream`
- **修复通知文字**：`room_enter` 根据 subtype 显示「发起/加入了 视频/语音 通话」

### 🔧 改进
- **调用栏移除**：蓝色通话横幅已删除，房间人数通过房间选择面板查看
- **屏幕共享关闭按钮**：仅隐藏窗口（共享继续），停止只能通过横幅按钮
- **网络质量检测**：disconnected 容忍（可自动恢复），仅 failed 才断连
- **聊天背景透明度**：白天模式覆盖层 0.92→0.88（背景图透出约 12%）
- **图标更新**：视频通话 📹（摄像机）、语音通话 📞（听筒）、随机决策 🎯

---

### 💥 重大变更
- **移除客户端分发**：删除 `ClientLauncher.java`、`src/scripts/`（客户端启动脚本）、`src/assembly/`（打包配置）、`start-client.*`、`release/` 目录。v2 起为纯 Web 应用，用户浏览器访问即可，不再需要下载客户端 JAR
- **Maven 构建简化**：移除 `maven-jar-plugin`（client-jar）、`maven-resources-plugin`（copy-static）、`maven-assembly-plugin`，仅保留 Spring Boot 插件

---

## v1.0.7 (2026-06-19)

### 🐛 Bug 修复
- **修复对方摄像头/语音无法接收**：`ontrack` 事件中 `event.track` 可能为 `undefined` 导致崩溃，后续 track 处理全部中断；增加空值保护并用 `event.streams[0]` 优先生获取浏览器原生关联流
- **修复 ontrack 多 track 覆盖问题**：`addRemoteTile` 在已有 tile 时仅更新 `srcObject`，不再重建 DOM 元素
- **修复每次进入通话降噪未重新应用**：`nrInitialized` 标志一次置 true 后不再重置，导致第二次进入通话时降噪跳过；改为每次进入通话始终调用 `enableNR`

### 🔧 改进
- **降噪改为后台静默模式**：移除通话界面中的降噪切换按钮，每次进入通话自动应用强力降噪；如需调节请在设置→语音测试中测试不同降噪等级
- **降噪整合为三档**：删除"标准"模式，全局统一为「关闭 / 柔和 / 强力」三档，图标 ○ / ◐ / ●
- **网络质量角标**：增加 RTT 延迟显示（格式 `●98% 12ms`），综合音频+视频丢包率计算，修正无数据时假性 100%
- **数学符号面板**：Markdown 工具栏新增 Ω 按钮，点击展开 55 个常用数学/LaTeX 符号，点击即插入到光标位置

---

## v1.0.6 (2026-06-19)

### ✨ 新功能
- **IP 白名单防护**：支持 IP 白名单过滤，`chat.ip-whitelist.enabled=true` 开启后仅允许白名单 IP 访问，`127.0.0.1` 始终放行，支持 `X-Forwarded-For` 代理头
- **消息频率限制**：服务端每用户每秒最多 10 条消息（chat/private/file），防止恶意刷屏，voice_data 中继不受限
- **屏幕共享状态查询**：`screen_status` 服务端权威查询接口，客户端点击共享时先问服务端再决定行为
- **新人屏幕共享通知**：`screen_active` 消息主动通知新加入用户当前活跃的屏幕共享
- **新人屏幕共享主动接入**：`screen_watch` + `screen_new_viewer` 双向信令，新人自动获得画面

### 🐛 Bug 修复
- **修复消息身份伪造**：新增 `isSessionOwner()` 校验，所有消息 handler 验证 WebSocket 连接与声称昵称一致，防止冒充在线用户发言
- **修复屏幕共享双重发起**：`handleScreenStart` 改为 `synchronized` 原子检查，三层防线（客户端本地→服务端查询→服务端原子拦截），任何人无法覆盖他人共享
- **修复屏幕共享后加入者黑屏**：`screen_active` 处理器自动调出查看窗口 + 发送 `screen_watch` 请求画面流；分享按钮在有人共享时变为「调出观看窗口」
- **修复屏幕共享 ICE 黑屏**：`screen_offer/answer/ice` 回滚为广播路由，保证 ICE 握手两端完整
- **修复启动脚本不重新编译**：所有启动脚本改为每次启动前增量编译 `mvn package`，彻底杜绝源码修改后运行旧 JAR
- **修复启动脚本编码错误**：所有 `.bat` 文件添加 `chcp 65001` 切换到 UTF-8 代码页
- **修复 v1.0.3 版本号残留**：`src/scripts/` 下脚本版本号统一修正为 v1.0.6

### 🔧 改进
- **TLS 误拨日志过滤**：`logging.level.org.apache.coyote.http11.Http11InputBuffer=OFF` 抑制 TLS 探针误拨 HTTP 端口的日志噪音
- **项目文件整理**：删除 `cloudflared.exe`(51MB)、过期备份 `web-bugfix-20260618/`、重复 `backup/uchat-v1.0.5-windows/`；`tools/` 目录收纳 Radmin VPN 安装包
- **构建脚本路径修正**：`build-release.bat` 中的 Radmin 路径更新为 `tools/Radmin_*`

---

## v1.0.5 (2026-06-18)

### ✨ 新功能
- **记住账号/密码/自动登录**：登录界面新增三个选框，凭据存 localStorage 持久化；勾选自动登录后下次打开页面自动连接
- **切换账号**：设置面板底部新增"退出登录"按钮，一键返回登录界面
- **Markdown 输入工具栏**：输入框上方 10 个格式按钮（加粗/斜体/删除线/代码/行内公式/块级公式/链接/列表/引用），Ctrl+B/I 快捷键，支持折叠
- **数学公式渲染**：集成 KaTeX，`$...$` 行内公式 + `$$...$$` 块级公式，暗色模式自动适配
- **搜索增强**：全部匹配项同步高亮、多关键词空格分隔、250ms 去抖、发言人选单自动补全、搜索历史（最近 10 条）、键盘 Enter/Shift+Enter 导航
- **用户状态标签**：右侧在线列表实时显示 📞通话中 / 🎵听歌房 / 🎙️语音 / 🖥️屏幕共享 / 正在输入
- **页面动画**：消息入场、弹窗、Toast、右键菜单等全面添加 CSS 动画

### 🐛 Bug 修复
- **修复设置持久化**：ClientLauncher 端口复用 + 杀旧进程，避免 origin 变化清空 localStorage；服务端备份链错误日志 + keepalive
- **修复发送按钮双重绑定**：统一用 addEventListener 替代 onclick，消除离线恢复后重复发送
- **修复文件上传同名覆盖**：重名文件自动追加时间戳后缀
- **修复 Ctrl+Enter 换行**：改为手动插入 `\n`，不再依赖浏览器默认行为
- **修复听歌房状态残留**：退出听歌房时向退出者单独发空状态，广播空房间时向全体在线用户发送
- **修复用户状态累积**：typingTimers 从 _userStatuses 分离为独立变量，避免 for-in 污染
- **修复 SSRF 漏洞**：URL 预览接口禁止内网/环回/链路本地/云元数据地址
- **修复 room.dat 格式校验**：加载时检查 name=/maxCapacity= 前缀，损坏文件不导致崩溃
- **修复暗色模式链接颜色**：全局 `<a>` 使用 --text-link 变量，不再显示浏览器默认深蓝
- **修复文件/语音消息字体不跟随**：appendFileMessage 也应用 savedFont/savedSize
- **修复 IME 输入法误发送**：keydown 加 e.isComposing 保护

### 🔧 改进
- **消息布局重构**：昵称+时间独立为 msg-header 行，内容在 msg-body 统一缩进，多行消息不再参差不齐
- **懒人包分平台**：Windows 包含内置 JRE（jlink 精简）+ Radmin 安装包；macOS 包独立打包
- **懒人包去服务端**：只分发客户端 JAR + 脚本，不再包含 uchat.jar
- **删除死代码**：移除语音消息（录制/播放）全链路、SignalingHandler 未注册端点、VBS 启动脚本
- **每条消息写磁盘优化**：Room.addHistory 改为 1秒节流 + 10秒定时兜底，减少 97%+ IO
- **番茄钟标题修复**：停止时自动恢复页面原始标题
- **服务端默认地址**：改为 26.136.34.205:8888

---

## v1.0.4 (2026-06-18)

### 🐛 Bug 修复
- **修复设置持久化不完整**：`loadAllPrefs()` 现在恢复所有设置项并写回各个 localStorage key
- **修复服务器备份不完整**：`saveUserPrefs()` 现发送全部设置到服务器
- **修复代码结构**：工具栏按钮显隐和麦克风/摄像头监听器移入独立函数
- **修复主题闪白**：页面加载时优先从 `chatroom-theme` key 恢复主题

---

## v1.0.3 (2026-06-18)

### ✨ 新功能
- **语音测试**：设置面板新增麦克风测试，支持实时音量显示（dB计量+彩色音量条）、侧音监听（听自己声音）、AI降噪模型实时对比（关/柔和/标准/强力）
- **环境音效**：独立浮动面板（🌧️按钮），雨声/咖啡馆/篝火/白噪音 4种背景音，全部 Web Audio API 合成无需音频文件
- **番茄钟**：🍅按钮弹窗，番茄模式（自定义工作/休息/轮次）、倒计时（精确到秒+预设）、正计时三种模式
- **随机决策**：🎲按钮弹窗，输入选项空格分隔→滚动动画随机抽取
- **自定义聊天背景**：设置面板上传图片，CSS半透明渐变叠加，自动适配亮/暗主题，IndexedDB持久化
- **刷新自动重连**：关闭标签页后重新打开自动登录（sessionStorage凭据+5分钟服务器宽限期）
- **发送习惯设置**：Ctrl+Enter发送 / Enter发送 自由切换，占位符跟随更新
- **工具栏按钮显隐**：设置面板底部可隐藏任意工具栏按钮（表情/骰子/文件/环境音/番茄钟/随机决策）
- **Enter换行支持**：聊天框默认Enter换行、Ctrl+Enter发送

### 🐛 Bug 修复
- **修复第二个用户加入语音时被踢下线**：Tomcat WebSocket 缓冲区 8KB→256KB，SDP超大导致1009断连
- **修复建连Glare**：仅新加入者发offer，消除双端同时建连冲突
- **修复广播风暴级联断连**：broadcastRoomState 加重入保护+独立try/catch
- **修复屏幕共享黑屏**：receiver变量未定义、getUserMedia误拦截、流获取兼容、显式play()
- **修复同机设备ID冲突**：localStorage key按昵称隔离
- **修复AI降噪底噪**：压缩器阈值提高+扩展器强化，强力模式3ms启动压制键盘声
- **修复6个崩溃/泄漏**：重连errorEl→toast兜底、msgIdMap/privateHistory上限、摄像头流泄漏、file.type空值、消息静默丢失
- **修复4个非原子写入**：users.dat/room.dat/prefs.json/文件上传全部写.tmp→ATOMIC_MOVE
- **修复设置持久化**：集中存储为单一JSON，localStorage主存储+服务器补全，不覆盖本地新值
- **修复私聊离线无提示**：服务端检查在线状态+客户端阻止选择离线用户

### 🔧 改进
- **删除桌面端框架**：移除JavaFX依赖，服务端JAR 27MB→18MB
- **目录结构重组**：构建工具入scripts/，根目录仅保留启动脚本
- **脚本全面同步**：bat/sh/vbs三套统一JAR名称和版本号，支持Tailscale IP自动检测
- **macOS懒人包**：.command双击启动器+.sh脚本
- **debug服务端**：start-server-debug.bat 端口8889，详细WebSocket帧日志
- **代码语法高亮**：集成highlight.js，\`\`\`代码块自动着色
- **URL预览卡片**：自动抓取OG标签显示标题+描述+缩略图
- **语音通话质量指示**：getStats()实时丢包率+延迟角标（绿/橙/红）
- **链接新标签打开**：markdown链接+URL预览卡片均target="_blank"
- **消息悬停半透明**：rgba(128,128,128,0.12)替代实色背景

---

## v1.0.0 (初始版本)
- 文字聊天（Markdown、@提及、私聊）
- 语音/视频通话（WebRTC Mesh）
- 屏幕共享
- 文件/图片上传与预览
- 听歌房（网易云音乐）
- 骰子、表情包
- 消息搜索、撤回、引用回复
